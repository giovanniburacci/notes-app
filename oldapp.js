/*const fs = require('fs');

/*const content = fs.readFileSync('notes.txt');
fs.writeFileSync('notes.txt', content + '\nYou suck'); manual append

fs.appendFileSync('notes.txt', '\nstringLine'); // smart append


const utils = require('./utils');

/*console.log(utils.name);
console.log(utils.add(1, 2));

console.log(utils.getNotes());

const validator = require('validator');
import validator from 'validator';
console.log(validator.isURL('https://2google.com'))

import chalk from 'chalk';

console.log(chalk.bgMagenta.bold.white("Alert!"));*/